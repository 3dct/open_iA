set(MODULE_DESCRIPTION
	"Meta filters: Apply some other filters in a specialized fashion:\n- Batch - Apply the same filter with same parameters to all images in a selected folder\n- Patch filter - Apply the same filter with the same parameters to small regions of interest (=patches) of the same image\n- Sample (to be done!) - Run the same filter with varying parameters on the same image"
)
